export const LESSONS = [
  {
    level: 1,
    title: "Leçon 1 : Lecture basique",
    content: "Bienvenue dans la première leçon MongoDB ! 🎓\n\n### Qu'est-ce que MongoDB ?\nMongoDB est une base de données NoSQL (orientée documents). Les données ne sont pas stockées dans des tables comme en SQL, mais dans des **collections** sous forme de **documents** (similaires au format JSON).\n\n### La méthode `find()`\nLa requête la plus courante est `find()`. Elle sert à chercher des documents dans une collection.\n* **Code SQL équivalent :** `SELECT * FROM clients;`\n* **En MongoDB :** `db.clients.find({})`\n\nLe `{}` au centre représente nos filtres de recherche. S'il est vide, on prend tout !\n\n### Filtrer les résultats (Projection)\nSi vous ne voulez récupérer que certains champs pour alléger l'affichage, vous pouvez utiliser le deuxième paramètre de `find`, appelé **projection**.\nOn utilise `1` pour inclure et `0` pour exclure (notamment pour le champ `_id` qui vient toujours par défaut).\nExemple : `db.clients.find({}, { nom: 1, _id: 0 })` (Renvoie tout le monde, mais uniquement avec leur nom).\n\n### Les autres fonctions utiles\n* `findOne({})` : Au lieu de renvoyer une liste, renvoie **uniquement** le premier document trouvé.\n* `countDocuments({})` : Pour compter précisément combien de documents correspondent à notre recherche.\n* `sort({})` et `limit()` : Pour trier (`1` pour croissant, `-1` pour décroissant) ou limiter le nombre de résultats. (ex: `db.clients.find({}).limit(5)`)",
    docsLink: "https://www.mongodb.com/docs/manual/tutorial/query-documents/"
  },
  {
    level: 2,
    title: "Leçon 2 : Filtres & Opérateurs",
    content: "Maintenant que l'on sait lire, apprenons à chercher précisément ! 🔍\n\n### Les opérateurs de comparaison\nEn MongoDB, les conditions se font sous forme d'objets JSON avec des opérateurs préfixés par `$ `.\n* **$gt** (greater than) : Strictement supérieur `>`\n* **$gte** (greater than or equal) : Supérieur ou égal `>=`\n* **$lt** (less than) : Strictement inférieur `<`\n* **$ne** (not equal) : Différent de `!=`\n\nExemple : `db.projets.find({ budget: { $gt: 5000 } })`\n\n### Chercher dans des tableaux\nMongoDB est puissant car il peut stocker des tableaux directement dans un document.\nSi vous cherchez une valeur précise dans un tableau, il vous suffit de le faire comme s'il s'agissait d'une valeur simple !\nExemple : `db.employes.find({ competences: \"React\" })` trouvera tous les employés ayant \"React\" dans leur liste de compétences.\n\nPour être plus spécifique :\n* **$in** : Vérifie si la valeur du champ correspond à l'une des valeurs d'un tableau (Comme le `IN` en SQL). Exemple : `{ statut: { $in: [\"En cours\", \"Terminé\"] } }`.\n* **$all** : Exige que **toutes** les valeurs spécifiées soient dans le tableau (mais l'ordre n'importe pas).",
    docsLink: "https://www.mongodb.com/docs/manual/reference/operator/query/"
  },
  {
    level: 3,
    title: "Leçon 3 : Modifications & Index",
    content: "Il est temps d'altérer vos données ! 🛠️\n\n### Insérer des données\nPour ajouter un ou plusieurs nouveaux documents dans une collection :\n* `insertOne({ nom: \"Nouveau Client\" })`\n* `insertMany([{ nom: \"Client A\" }, { nom: \"Client B\" }])`\n\n### Mettre à jour des données (Update)\nPour modifier, on utilise `updateOne()` (ou `updateMany()`).\n**Attention !** Si vous ne précisez pas quel type de mise à jour faire, MongoDB peut écraser tout le document.\n* **$set** : Modifie (ou ajoute) uniquement les champs spécifiés.\n* Exemple pour mettre à jour le budget du projet \"Alpha\" :\n  `db.projets.updateOne({ titre: \"Alpha\" }, { $set: { budget: 10000 } })`\n\n### Modifier des tableaux\nPour insérer `$push` ou enlever `$pull` un élément d'un tableau contenu dans un document :\n* `{ $push: { competences: \"MongoDB\" } }`\n\n### Supprimer des données\n* `deleteOne({ ... })` ou `deleteMany({ ... })`.  Si le filtre est vide `deleteMany({})`, attention ça efface tout le contenu !\n\n### L'indexation\nPour accélérer les recherches fréquentes, on utilise `- createIndex()`.\n`db.clients.createIndex({ nom: 1 })` (l'index est créé de façon ascendante sur le champ \"nom\").",
    docsLink: "https://www.mongodb.com/docs/manual/crud/"
  },
  {
    level: 4,
    title: "Leçon 4 : L'Agrégation (Bases)",
    content: "L'agrégation permet de traiter des données et de renvoyer des résultats calculés 📊.\n\n### Qu'est-ce qu'un pipeline d'agrégation ?\nC'est un tableau d'étapes (stages) successives par lesquelles passent les documents.\nOn utilise la commande `aggregate([...])`.\n\n1. **$match** : C'est comme le `find()`. Il filtre les documents pour ne garder que ceux voulus (souvent la première étape pour optimiser !).\n2. **$group** : Regroupe les documents par une ou plusieurs clés spécifiées.\n   L'identifiant du groupe s'appelle toujours `_id`.\n   Exemple : grouper par secteur `{ $group: { _id: \"$secteur\", ... } }`. (On met un `$` devant le nom du champ pour indiquer qu'on lit sa valeur).\n\n### Accumulateurs\nDans l'étape `$group`, on peut appliquer des fonctions mathématiques :\n* **$sum** : Additionne (souvent utilisé avec `1` pour compter). Ex: `total: { $sum: 1 }`\n* **$avg** : Calcule la moyenne d'un champ. Ex: `moyenne_budget: { $avg: \"$budget\" }`\n\n### Refaçonner avec $project\nL'étape **$project** permet de renommer, formater ou manipuler l'apparence des documents pour l'étape suivante.",
    docsLink: "https://www.mongodb.com/docs/manual/core/aggregation-pipeline/"
  },
  {
    level: 5,
    title: "Leçon 5 : Pipelines Complexes",
    content: "On attaque les notions avancées façon \"Jointures SQL\" ! 🔗\n\n### Les Jointures avec $lookup\nEn NoSQL, il n'est pas interdit d'utiliser des références entre collections ! L'étape **$lookup** permet de récupérer les données d'une autre collection.\nSyntaxe de base :\n```json\n{\n  $lookup: {\n    from: \"autres_collection\",\n    localField: \"mon_champ_id\",\n    foreignField: \"_id\",\n    as: \"resultats_lies\"\n  }\n}\n```\n\n### Aplatir avec $unwind\n`$lookup` renvoie systématiquement les données jointes sous la forme d'un **tableau** (car MongoDB ne sait pas s'il y aura 1 ou 1000 résultats).\nSi on gère une relation \"Un-à-Un\" et qu'on veut \"sortir\" les objets du tableau pour simplifier l'accès, on utilise **$unwind**.\n`{ $unwind: \"$resultats_lies\" }` va créer un document distinct pour chaque élément du tableau. S'il y a un élément unique, ça supprime simplement le tableau !",
    docsLink: "https://www.mongodb.com/docs/manual/reference/operator/aggregation/lookup/"
  }
];